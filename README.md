# Автотесты кнопок получния копии книги и закрытия фрейма для компании Systeme.io

Набор автоматизированных UI-тестов для проверки всплывающего окна на странице:  
👉 https://systeme.io/blog/cost-of-online-course

⚠️ **Важно**:  
- По умолчанию тесты запускаются **локально**

Тесты проверяют:
- Отображение кнопки «I want to receive my copy»
- Корректную работу кнопки закрытия всплывающего окна

---

## 🛠 Требования

- **Локально**:  
  - Java 17+  
  - Maven 3.8+  
  - Google Chrome (установленный в системе)

- **В Docker**:  
  - Docker Desktop (запущен)

---

## ▶️ Запуск локально

1. Откройте терминал в корне проекта.
2. Выполните:

```bash
mvn clean test
```
**Тесты запустятся в headless-режиме (браузер не будет отображаться).**
- Чтобы увидеть браузер во время выполнения — измените Configuration.headless = false в файле конфигурации (BaseTest.java).

---

## 🐳 Запуск в Docker
1. Раскомментируйте следующую строку в файле [`BaseTest.java`](src/test/java/utils/BaseTest.java):
```java
Configuration.remote = "http://localhost:4444";
```
3. Соберите Docker-образ:
```bash
docker build -t testforsystemeio .
```
3. Запустите тесты:
```bash
docker run --rm testforsystemeio
```



